#ifndef MUTEX_H
#define MUTEX_H

#include "../atomic.h"

// For compatibility with filesystem code.
// 😭😭😭😭
namespace std {
    using mutex = SpinLock;
    template <typename T>
    using lock_guard = LockGuard<T>;
}

#endif  // MUTEX_H